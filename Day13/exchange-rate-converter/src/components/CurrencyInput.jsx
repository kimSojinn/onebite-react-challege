import React from "react";

const CurrencyInput = ({ label }) => {
    return (
        <div>
            <span>{label} </span>
            <input></input>
        </div>
    );
};

export default CurrencyInput;
